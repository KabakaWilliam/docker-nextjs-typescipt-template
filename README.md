## DOCKER-TAILWIND-NEXTJS-TYPESCRIPT TEMPLATE

First, run the development server:

```bash
npm i
```

Run the docker image

```bash
docker-compose -f docker-compose.production.yml up --build --force-recreate

```
